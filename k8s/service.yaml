apiVersion: v1
kind: Service

metadata:
  name: spring-boot-dockercompose-service

spec:
  type: NodePort

  selector:
    app: spring-boot-dockercompose

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080